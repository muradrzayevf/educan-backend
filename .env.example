PORT=4000
FRONTEND_URL=http://localhost:5173

DATABASE_URL=postgres://postgres:password@localhost:5432/educan
DB_SSL=false

JWT_SECRET=
JWT_EXPIRES_IN=

RESET_URL_BASE=http://localhost:5173/reset-password
PAYMENT_PROVIDER=dodo

# Bankın verdiyi login/parol (test sandbox: TerminalSys/kapital + kapital123)
DODO_ENV=test                 # test → test.dodopayments.com ; live → live.dodopayments.com
DODO_API_KEY=               # GİZLİ — Dashboard → Developer → API (test açarı 'test_...')
DODO_WEBHOOK_SECRET=       # GİZLİ — Dashboard → Developer → Webhooks ('whsec_...')
DODO_PRODUCT_ID=       # Dashboard → Products → məhsulun ID-si (pdt_.../prod_...)
DODO_PWYW=true     # sandbox: USD (Square AZN dəstəkləmir)
# SQUARE_VERSION=2025-01-23




# ===== Zoom (Server-to-Server OAuth) =====
# Boş qalsa placeholder meeting linki istifadə olunur (lokal/test).
# Real üçün marketplace.zoom.us-də "Server-to-Server OAuth" app yarat.
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

# ===== Email (SMTP) =====
# Boş qalsa email-lər konsola yazılır (lokal/test).
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=EduCan <no-reply@educan.az>
GOOGLE_CLIENT_ID=

STORAGE=r2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=educan
R2_PUBLIC_URL=https://pub-<R2_ACCOUNT_ID>.r2.dev